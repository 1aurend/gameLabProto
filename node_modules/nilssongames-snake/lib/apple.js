function apple(game) {
  this.size = 15;
  this.x = Math.floor(Math.random() * (game.board.width / this.size)) * this.size + game.board.x;
  this.y = Math.floor(Math.random() * (game.board.height / this.size)) * this.size + game.board.y;

  this.draw = function (context) {
    context.fillStyle = '#dd3d27';
    context.fillRect(this.x, this.y, this.size, this.size);
  };
}

var _default = apple;
export default _default;
;

var _temp = function () {
  if (typeof __REACT_HOT_LOADER__ === 'undefined') {
    return;
  }

  __REACT_HOT_LOADER__.register(apple, 'apple', 'src/apple.js');

  __REACT_HOT_LOADER__.register(_default, 'default', 'src/apple.js');
}();

;
