var draw = {
  countdown: function countdown(context, game) {
    var canvasSize = game.canvasSize,
        countdown = game.countdown,
        time = game.time;

    var currentCountdown = countdown - parseInt(time / 1000);

    if (currentCountdown < 1) {
      return;
    }

    context.font = '50px Arial';
    context.textAlign = 'center';
    context.fillStyle = '#888';
    context.fillText(currentCountdown, canvasSize / 2, canvasSize / 2);
  },

  gameOver: function gameOver(context, canvasSize) {
    context.font = '40px Arial';
    context.textAlign = 'center';
    context.fillStyle = '#888';
    context.fillText('Game over', canvasSize / 2, canvasSize / 2);
  },

  score: function score(context, game) {
    context.fillStyle = '#444';
    context.fillRect(0, 0, game.canvasSize, game.canvasSize - game.board.height);

    context.font = '16px Arial';
    context.textAlign = 'left';
    context.fillStyle = '#eee';
    context.fillText('Score: ' + game.score, 10, 25);

    context.textAlign = 'right';
    context.fillText('High Score: ' + game.highScore, game.canvasSize - 10, 25);
  }
};

var _default = draw;
export default _default;
;

var _temp = function () {
  if (typeof __REACT_HOT_LOADER__ === 'undefined') {
    return;
  }

  __REACT_HOT_LOADER__.register(draw, 'draw', 'src/draw.js');

  __REACT_HOT_LOADER__.register(_default, 'default', 'src/draw.js');
}();

;